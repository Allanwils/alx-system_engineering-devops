#!/bin/env bash

# Installs Nginx, configures it to add a custom header to its HTTP response.
sudo apt update
sudo apt install -y nginx

SERVER_HOSTNAME="102967-web-01"

echo "add_header X-Served-By $SERVER_HOSTNAME;" | sudo tee -a /etc/nginx/nginx.conf

sudo systemctl restart nginx
